# This Next.js application is configured for static export.
# Firebase App Hosting will automatically detect the 'out' directory
# and serve it as a static site. No further configuration is needed here.
